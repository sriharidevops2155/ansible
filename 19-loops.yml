- name: install packages
  hosts: frontend
  become: yes 
  tasks:
  - name: install packages 
    package:
      name: "{{ item.a }}"
      state: "{{ item.b }}"
    loop:
    - {a: mysql ,b: installed}
    - {a: nginx ,b: installed}
    - {a: zip ,b: installed}
